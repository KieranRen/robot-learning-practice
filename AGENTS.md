# 个人学习工作区约定

本文件适用于复制模板后建立的个人实践仓库。此仓库用于学习、试验、记录进度和保存证据。

## 工作范围

- 修改前阅读 README.md、progress.md、相关代码与测试，检查当前 Git 状态
- 个人代码、笔记和报告保存在本仓库，教材副本按只读用途使用；默认 ../knowledge，并行使用其他副本时以任务记录为准
- 需要公共贡献时转到单独的 ../contribution，从当前上游 main 建立任务分支
- 默认教材来源为 https://github.com/noBug01/Robot_knowledge_study，首版标签为 v0.1.0；以 README 和逐任务记录核对实际来源、标签及完整 commit，不据默认值推定已有记录
- 教材仓库为私有；含教材内容的个人远程保持相应访问限制，不自动公开。贡献使用已授权协作分支或允许的私有 Fork，不假定读取权限包含 Fork 或写入权限
- 不把个人练习、未经整理的笔记或环境报告直接加入公共知识库
- 参与者可以随时将发现整理为公共贡献，说明复现条件、证据与可复用价值，无需先完成某个学习阶段
- 不覆盖已有修改，不改变教材副本的版本，不擅自修改 Git remote 或全局配置
- 创建远程仓库、push 或合并 PR 前，需要用户对目标和操作的明确授权

## 学习与验证

- 保留公式、假设、输入输出和中间结果，帮助学习者理解关键实现
- 新行为配对应测试，先运行相关示例，再检查差异
- 路径使用 pathlib，不在代码中固定本机绝对路径
- Python 支持范围遵循所用教材版本，公共函数补 type hints 和简洁 docstring
- 不提交密钥、个人凭据、数据集、模型权重或大型生成结果
- 保留复制文件的来源、版权声明与适用许可证
- 按任务类型检查证据，概念任务保留解释、推导或反例，实现任务运行相关测试，实验任务保存配置、指标与误差或失败分析；完整 M0 验收仍遵循教材原有清单

## 进度记录

每项任务在 progress.md 中记录稳定任务 ID、教材来源和参考副本、tag 与完整 commit、实际运行条件或环境报告、状态、证据、评审和下一步。可以并行记录多个真实目标，不能把未建设模块的规划说明写成已执行的教材任务。

顶部教材与环境摘要不能覆盖逐任务记录。不同教材版本并行使用时，核对各任务对应的参考副本与当前提交，不把同一目录视作同时处于两个版本，不擅自切换其他任务所依赖的副本或环境。概念任务确无运行条件时注明不适用。

升级教材时追加来源和任务记录，保留已完成任务的旧版本依据。旧模板可以追加新字段或说明，不能整份覆盖已有记录；缺失的历史信息只补可核实内容。成果证据标明实际个人提交，存在未提交改动时说明范围。

完成遵循 read → understand → test → review。学习者需要阅读关键修改、解释实现并验收结果。AI 不能自行把未执行的检查或未经人确认的理解写成已完成。

## 常用验证

以下命令用于初始 M0 工作区，从个人仓库根目录运行。其他模块按所用教材中的实际任务入口检查。

```bash
python examples/hello_robot.py
python -m pytest
git diff --check
git status --short
```
